---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<main>
			<div class="hero-container">
				<div class="hero-text">
					<h1 class="hero-title">Cloudflare Atlas</h1>
					<p class="hero-subtitle">All your docs in one place</p>
				</div>
				<div class="hero-image">
					<img src="/atlas-logo-v1.png" alt="Cloudflare Atlas Logo" />
				</div>
			</div>
		</main>
		
		<!-- Add some content to make the page scrollable -->
		<section style="height: 200vh; background: linear-gradient(to bottom, #ffd7b5, #ffb88c); padding: 2rem; display: flex; align-items: center; justify-content: center;">
			<div style="text-align: center; color: #333;">
				<h2 style="font-size: 3rem; margin-bottom: 1rem;">Scroll to see the atlas logo spin!</h2>
				<p style="font-size: 1.5rem;">The logo completes a full 360° rotation from top to bottom of this page.</p>
			</div>
		</section>

		<script src="https://unpkg.com/animejs@3.2.1/lib/anime.min.js"></script>
		<script>
			window.addEventListener('load', function() {
				console.log('Page loaded');
				
				// Simple scroll rotation without anime.js first
				window.addEventListener("scroll", function () {
					const scrolled = window.pageYOffset;
					const rotation = scrolled * 0.5;
					
					console.log('Scroll:', scrolled, 'Rotation:', rotation);
					
					const logoImg = document.querySelector(".hero-image img");
					if (logoImg) {
						logoImg.style.transform = `rotate(${rotation}deg)`;
					}
				});
			});
		</script>

		<style>
			body {
				margin: 0;
				padding: 0;
				height: 100vh;
				overflow-x: hidden;
				background-color: #ffd7b5;
			}

			main {
				height: 100%;
				width: 100%;
				max-width: 100vw;
				overflow-x: hidden;
				background-color: #ffd7b5;
			}
			.hero-container {
				display: block;
				min-height: 100vh;
				width: 50%;
				padding: 0;
				box-sizing: border-box;
			}

			.hero-text {
				display: flex;
				flex-direction: column;
				justify-content: center;
				min-height: 100vh;
				text-align: left;
				padding: 4rem;
				box-sizing: border-box;
			}

			.hero-title {
				font-size: clamp(3rem, 8vw, 6rem);
				font-weight: bold;
				margin: 0 0 1rem 0;
				line-height: 1.1;
				color: #333;
			}

			.hero-subtitle {
				font-size: clamp(1.2rem, 3vw, 2rem);
				margin: 0;
				color: #666;
				font-weight: 300;
			}

			.hero-image {
				position: fixed;
				top: 0;
				right: 0;
				width: 50%;
				height: 100vh;
				display: flex;
				justify-content: center;
				align-items: center;
				z-index: 1;
			}

			.hero-image img {
				width: 65%;
				height: auto;
				max-width: 100%;
				object-fit: contain;
			}

			@media (max-width: 768px) {
				.hero-container {
					flex-direction: column;
					text-align: left;
					min-height: auto;
					padding: 1rem;
				}

				.hero-text {
					order: 1;
					text-align: left;
				}

				.hero-image {
					order: 2;
					max-width: 80%;
				}
			}
		</style>
		<Footer />
	</body>
</html>
